@page
@model AcadPrep.WebUI.Pages.Account.GoogleCallbackModel
@{
    // Trang này không render gì — OnGetAsync luôn redirect trước khi render.
    // File cần tồn tại để Razor Pages routing hoạt động.
}
